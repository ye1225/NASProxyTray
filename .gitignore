# 构建产物
dist/
dist_backup/
_pack/
build/
bin/
obj/

# WebView2 运行时数据
.webview2/
EBWebView/
*.ldb
*.sst

# 日志 / 临时
*.log
log.txt
run.log
tmp/
*.tmp
*.bak

# 可执行文件
*.exe
*.pdb
*.suo
*.user

# 注意：这里故意不写 *.dll，因为 lib/ 下的 3 个 DLL 需要提交

# 编辑器 / 系统
.vscode/
.idea/
*.swp
*~
Thumbs.db
Desktop.ini

# 本地敏感配置
config.local.json
*.secret
.env

# ========== 用户运行时数据（每台机器不一样，不入库）==========
config.json
proxy.pac
proxy_remote.pac

# ========== 助手工作目录（本地记忆/任务，不入库）==========
.workbuddy/


# ========== 检查更新的节流记录（跟着版本走，不入库）==========
update_check.json
